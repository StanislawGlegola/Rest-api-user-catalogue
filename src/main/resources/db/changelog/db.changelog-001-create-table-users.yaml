databaseChangeLog:
  - changeSet:
      id: Create table users
      author: sglegola
      changes:
        - sql: |
            CREATE TABLE `users` (
              `id` INT NOT NULL AUTO_INCREMENT,
              `userName` VARCHAR(200) NULL,
              `age` INT NULL,
              PRIMARY KEY (`id`)
            );